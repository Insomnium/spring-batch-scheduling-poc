# spring-batch-scheduling-poc
Scheduling spring batch job in distributed environment with reentrant lock on single instance. 
Two ways (Quartz & Shedlock based) are in `feature/quarts` & `feature/shedlock` branches
